import processing.core.PImage;
import java.util.List;
import java.util.Optional;

public class Fence extends Entity
{
    public Fence(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
